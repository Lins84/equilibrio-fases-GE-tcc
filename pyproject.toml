[project]
name = "python-template"
version = "0.1.0"
description = ""
authors = [{ name = "Leonardo de Sousa Lins" }]
requires-python = ">=3.11"
dependencies = [
    "flet>=0.86.2",
    "flet-charts>=0.86.2",
    "numpy>=2.3.2",
    "thermo>=0.4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["calculos"]
